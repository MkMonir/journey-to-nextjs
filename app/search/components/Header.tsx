import SearchBar from "@/app/components/SearchBar";

const Header = () => {
  return (
    <header className="p-3 flex justify-center bg-gradient-to-r from-[#0f1f47] to-[#5f6984] ">
      <SearchBar />
    </header>
  );
};

export default Header;
